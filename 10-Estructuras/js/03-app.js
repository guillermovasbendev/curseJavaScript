// el operador mayor que y menor que ><
const dinero = 500;
const totalAPagar = 300;

if (dinero > totalAPagar) {
  console.log(`tenemos mas dinero quelo que se paga`);
} else {
  console.log(`fondos insuficientes para pagar
    `);
}
