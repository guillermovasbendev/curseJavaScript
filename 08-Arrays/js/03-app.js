const meses = [`enero`, `febrero`, `marzo`, `abril`, `mayo`, `junio`];
console.table(meses);
// saber la extension del array
console.log(meses.length);

// para listar elementos de un array mediante un for
// inicializa variable en 0, establece condicion , y suma 1
for (i = 0; i < meses.length; i++) {
  console.log(meses[i]);
}
