const numeros = [10, 20, 30, 40, 50, [3, 5, 6, 9]];
console.log(numeros);
console.table(numeros);
// table permite apreciar los datos en tablas mostrando index y value

// para acceder al arreglo usas el indice entre corchetes[]
console.log(numeros[3]);
// Para acceder un arreglo de arreglos usamos doble indice
console.log(numeros[5][2]);
